import React from "react";

const Home = () => {
  return (
    <div>
      <div>Learn React SSR</div>
      <button onClick={() => alert("React SSR")}>click</button>
    </div>
  );
};

export default Home;
